const email=new URLSearchParams(location.search).get('staff');const profiles=JSON.parse(localStorage.getItem('weblox-staff-portfolios')||'{}');const staff=JSON.parse(localStorage.getItem('weblox-master-staff')||'[]').find(x=>x.email===email);const p=profiles[email];const $=id=>document.getElementById(id);function text(el,value){el.textContent=value||''}if(!staff||!p){$('portfolio').innerHTML='<span class="eyebrow">PORTFOLIO UNAVAILABLE</span><h1>This portfolio link is unavailable.</h1><p class="public-about">The portfolio may not have been published in this browser yet.</p><a href="/">Return to WEBLOX Studios</a>'}else{text($('headline'),p.headline||staff.name);text($('about'),p.about);text($('location'),p.location);text($('staffName'),staff.name);(p.skills||[]).forEach(s=>{const tag=document.createElement('span');tag.textContent=s;$('skills').append(tag)});(p.projects||[]).forEach(project=>{const card=document.createElement('article');const h=document.createElement('h3');h.textContent=project.title;card.append(h);if(project.description){const d=document.createElement('p');d.textContent=project.description;card.append(d)}if(project.url){const a=document.createElement('a');a.href=project.url;a.target='_blank';a.rel='noreferrer';a.textContent='View project →';card.append(a)}$('projects').append(card)});if(!(p.projects||[]).length){const msg=document.createElement('p');msg.className='portfolio-missing';msg.textContent='No projects have been added yet.';$('projects').append(msg)}}document.documentElement.dataset.theme=localStorage.getItem('weblox-theme')||'dark';
